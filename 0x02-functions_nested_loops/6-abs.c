#include <stdio.h>
#include "main.h"
/**
 *_abs - Entry point
 *@c : interger to generate absolute value
 *Description:Returns the absolute value of a number
 *Return: 0 or 1 (Success)
 */
int _abs(int c)
{
	return (c >= 0 ? c : c c * -1);

}
