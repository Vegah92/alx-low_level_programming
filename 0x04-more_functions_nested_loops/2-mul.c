#include "main.h"

/**
 *mul - Entry point
 *@a: two numbers to be multiplied
 *@b: number to be multiplies
 *Description:A function that multiply two numbers
 *Return: (a * b)
 */

int mul(int a, int b)
{
return (a * b);
}
